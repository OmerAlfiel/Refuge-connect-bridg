<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>New Announcement</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background-color: #f5f5f5;
      padding: 20px;
      border-radius: 5px 5px 0 0;
      text-align: center;
    }
    .content {
      padding: 20px;
      background-color: #fff;
      border: 1px solid #ddd;
    }
    .important {
      background-color: #fff8f8;
      border-left: 4px solid #ff3333;
      padding: 10px 15px;
      margin-bottom: 20px;
    }
    .event-date {
      font-weight: bold;
      margin-top: 15px;
    }
    .footer {
      font-size: 12px;
      text-align: center;
      margin-top: 20px;
      color: #777;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>{{#if important}}🔴 IMPORTANT {{/if}}Announcement: {{ title }}</h1>
  </div>
  
  <div class="content">
    {{#if important}}
    <div class="important">
      <strong>This is an important announcement.</strong>
    </div>
    {{/if}}
    
    <p>{{ content }}</p>
    
    <p><strong>Category:</strong> {{ category }}</p>
    <p><strong>Region:</strong> {{ region }}</p>
    
    {{#if eventDate}}
    <p class="event-date">📅 Event Date: {{ eventDate }}</p>
    {{/if}}
  </div>
  
  <div class="footer">
    <p>You received this email because you are subscribed to announcements from Refuge Connect Bridge.</p>
    <p>To unsubscribe, click <a href="{{unsubscribeUrl}}">here</a>.</p>
  </div>
</body>
</html>